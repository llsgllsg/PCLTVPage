<StackPanel Orientation="Horizontal" Margin="0,2,0,12">
    <TextBlock Text="{label}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource ColorBrush2}" VerticalAlignment="Center" />
    <local:MyButton Margin="15,0,0,0" Height="28" Padding="15,0,15,0"
                    Text="刷新"
                    EventType="刷新页面"
                    EventData="-" />
</StackPanel>
