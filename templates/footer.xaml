<local:MyCard Margin="5,18,5,5" UseAnimation="False">
    <Grid Margin="14,10">
        <TextBlock TextWrapping="Wrap" TextAlignment="Center" HorizontalAlignment="Center"
                   FontSize="14" FontWeight="Bold"
                   Foreground="Black"
                   Text="作者:llsgllsg  ·  本主页使用 GPL-3.0 协议" />
    </Grid>
</local:MyCard>
</StackPanel>
